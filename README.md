# libcamera example app

This app uses libcamera and qt-5 to run stream images from webcam to screen
using libcamera.

# Requirements

* libcamera
* Qt5

# Build and Run

```bash
make
./libcamera-test-app
```

